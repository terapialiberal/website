# Netlify Configuration for Terapia Liberal
# Uses Jekyll to process CMS articles and auto-generate articles.json

[build]
  command = "bundle exec jekyll build"
  publish = "_site"

# Redirect for clean URLs (only for non-Jekyll paths)
[[redirects]]
  from = "/admin/*"
  to = "/admin/:splat"
  status = 200
